interface IColors {
  primaryBackgroundColor: string;
  secondaryBackgroundColor: string;
  primaryTextColor: string;
}

export const Colors: IColors = {
  primaryBackgroundColor: '#282A36',
  secondaryBackgroundColor: '#ff9f55',
  primaryTextColor: '#FFF',
};
